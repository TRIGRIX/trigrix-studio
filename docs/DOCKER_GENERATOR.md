# Docker generator

The generator creates an aiogram 3 long-polling application, slim Python 3.13 image, non-root user image, `restart: unless-stopped`, `.env.example` and Docker instructions CLI/Portainer. Ports and databases are not used.

