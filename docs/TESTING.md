# Testing

Unit tests cover IO, validation, Context Capsule, IR, routing and generators. A neutral web studio template opens as a golden project, compiled and exported to both platforms. Generated Python is validated through `compile()` without network running.

```powershell
pytest
```
