# Development Node

Node Registry is the only public block registration point. `NodeDefinition` specifies `type`, header, category, ports, Pydantic settings model, simulator handler and compiler handler. Type is allowed in the library only if there is validation, serialization, simulation and generation for both platforms.

You cannot add arbitrary Python from the project file.

