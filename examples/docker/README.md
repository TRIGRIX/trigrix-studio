# Web studio - application for the project — Docker

Autonomous bot generated by TRIGRIX Studio. No Builder is needed to run.

1. Copy `.env.example` into `.env`.
2. Complete the mandatory variables.
3. Do `docker compose up -d --build`.

Details: [DEPLOY_DOCKER.md](DEPLOY_DOCKER.md).
